<% provide(:title, @product.name) %>
<div class="row">
	<section class="span4">
		<h1>
			<%= @product.name %>
		</h1>
  <div>
    <%= link_to "Edit #{@product.name}", edit_product_path %>
  </div>
	</section>
	<section class="span4">
			<%= render @product %>
	</section>

</div>