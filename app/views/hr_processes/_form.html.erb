<div class="row">
	<div class="col-md-6	col-md-offset-3">
		<%= form_for(@hr_process) do |f| %>
			<%= render 'shared/error_messages', object: f.object %>

		<div class="row">
			<div class="form-horizontal">
				<div class="form-group">
					<%= f.label :hr_process_tag, "HR Process Name", class: "col-md-4 control-label" %>
					<div class="col-md-8">
						<%= f.text_field :hr_process_tag, class: "form-control" %>
					</div>
				</div>
			</div>
		</div>

		<div class="row">
			<div class="h1 col-md-offset-6">
				<%= f.submit "Submit", class: "btn btn-lg btn-primary" %>
			</div>
		</div>
			
		<% end %>
	</div>
</div>

